import os

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
TEST_DIR = os.path.join(BASE_DIR, "test_images")

#This script is only for path and directory check.
#Any other library checks and test commands can also be made it.
#It is not related to main code.(Detect.py)
